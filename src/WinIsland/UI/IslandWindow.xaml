<Window x:Class="WinIsland.UI.IslandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:WinIsland.UI.Converters"
        xmlns:karaoke="clr-namespace:WinIsland.UI"
        Title="WinIsland"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Width="400" Height="400"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MenuGlass.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisInvert" Invert="True"/>
        <conv:NullToVisibilityConverter x:Key="NullToVis"/>
        <conv:NullToVisibilityConverter x:Key="NullToVisInvert" Invert="True"/>
        <conv:Mdl2GlyphFontConverter x:Key="GlyphFont"/>

        <!-- Icon button (transparent, hover highlight) -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{Binding ButtonHoverBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#22FFFFFF}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Thin progress slider -->
        <Style x:Key="ThinSlider" TargetType="Slider">
            <Setter Property="Height" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border x:Name="Track" Height="4" CornerRadius="2"
                                    Background="{Binding SliderTrackBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#66FFFFFF}"/>
                            <Border x:Name="Fill" Height="4" CornerRadius="2" HorizontalAlignment="Left"
                                    Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="PART_Thumb" Width="12" Height="12" Margin="0">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse x:Name="thumb" Fill="{Binding SliderThumbBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                                         Stroke="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}" StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Thumb" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter TargetName="PART_Thumb" Property="Opacity" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 逐句卡拉OK歌词：非当前句淡化，当前句大号醒目（20px），字号/透明度平滑过渡 -->
        <Style x:Key="LyricLineText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,7"/>
            <Setter Property="LineHeight" Value="24"/>
            <Setter Property="Opacity" Value="0.28"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="FontSize" To="20" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="FontSize" To="13" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.28" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- 歌词行容器：不加背景胶囊，当前句高亮只由卡拉OK文字渐变体现（避免双重高亮） -->
        <Style x:Key="LyricLineBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="Root" Background="{x:Null}">
        <!--
            仿 iOS 灵动岛：窗口固定 400x400，仅内部卡片形变。
            紧凑 = 340x56 胶囊；展开 = 400x~370 卡片，向下生长。
            透明区域点击穿透（WPF 按渲染内容命中测试）。
        -->
        <Border x:Name="Card" Width="360" Height="72" CornerRadius="28"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                ClipToBounds="True"
                Background="{Binding CardBackground, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#101018}"
                BorderBrush="{Binding CardBorder, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#33FFFFFF}"
                BorderThickness="1"
                AllowDrop="True"
                DragOver="Card_DragOver" DragLeave="Card_DragLeave" Drop="Card_Drop"
                ContextMenuOpening="Card_ContextMenuOpening">
            <Border.ContextMenu>
                <ContextMenu x:Name="IslandMenu">
                    <MenuItem x:Name="MenuCenterAlign" Header="居中对齐" Click="MenuCenterAlign_Click"/>
                    <MenuItem x:Name="MenuLock" Header="上锁" Click="MenuLock_Click"/>
                    <Separator/>
                    <MenuItem x:Name="MenuOnlineLyrics" Header="在线歌词（免配置）" IsCheckable="True" Click="MenuOnlineLyrics_Click"/>
                </ContextMenu>
            </Border.ContextMenu>

            <Grid>
                <Grid x:Name="ContentGrid" Margin="10,6,10,6" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ═══ 胶囊行（始终可见）═══ -->
                <Grid x:Name="PillRow" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- 上岛推送（第三方软件推送到灵动岛）：紧凑态即展示全部内容，尺寸自适应 -->
                    <Border x:Name="CompactPushCard" Visibility="{Binding HasActivePush, Converter={StaticResource BoolToVis}}"
                            CornerRadius="14"
                            Background="{Binding PushCardBackground, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#221B1B26}"
                            BorderBrush="{Binding PushCardBorder, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#59FFFFFF}"
                            BorderThickness="1"
                            Padding="12,8" Margin="0,0,10,0" VerticalAlignment="Center">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="CompactPushScale" ScaleX="0.94" ScaleY="0.94"/>
                        </Border.RenderTransform>
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="30" Height="30" CornerRadius="9" Margin="0,0,8,0" ClipToBounds="True"
                                        Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}">
                                    <Grid>
                                        <TextBlock Text="{Binding ActivePushIcon}" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="White"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Visibility="{Binding ActivePushHasImage, Converter={StaticResource BoolToVisInvert}}"/>
                                        <Image Source="{Binding ActivePushImageSource}" Stretch="UniformToFill"
                                               Visibility="{Binding ActivePushHasImage, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </Border>
                                <TextBlock Text="{Binding ActivePushTitle}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"
                                           Foreground="{Binding PushCardForeground, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                           TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                                                            </StackPanel>
                            <TextBlock Text="{Binding ActivePushSubtitle}" FontSize="10.5" Margin="0,2,0,0"
                                       Foreground="{Binding PushCardSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                       TextTrimming="CharacterEllipsis" MaxWidth="150"
                                       Visibility="{Binding ActivePushHasSubtitle, Converter={StaticResource BoolToVis}}"/>
                            <TextBlock Text="{Binding ActivePushBody}" FontSize="11.5" Margin="0,5,0,0" TextWrapping="Wrap" MaxWidth="420"
                                       Foreground="{Binding PushCardSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                       Visibility="{Binding PushShowBody, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"/>
                            <ProgressBar Height="3" Maximum="1" Value="{Binding ActivePushProgress, Mode=OneWay}" Margin="0,6,0,0"
                                         Visibility="{Binding PushShowProgress, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border CornerRadius="2" Background="#33FFFFFF"/>
                                            <Border x:Name="PART_Indicator" CornerRadius="2" HorizontalAlignment="Left"
                                                    Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                            <ItemsControl ItemsSource="{Binding ActivePushButtons}" Margin="0,6,0,0"
                                          Visibility="{Binding PushShowButtons, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource IconButton}" Padding="9,4" Margin="0,0,8,0" Click="PushButton_Click">
                                            <TextBlock Text="{Binding Label}" FontSize="11.5"/>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- 按「组件顺序」动态渲染：歌曲信息 / 时间 / 天气（播放时含歌曲，空闲时去掉） -->
                    <ItemsControl ItemsSource="{Binding CompactItems}" VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Resources>
                            <DataTemplate x:Key="TimeItem">
                                <TextBlock Text="{Binding DataContext.ClockText, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           FontSize="{Binding DataContext.WidgetTimeFontSize, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"
                                           Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"/>
                            </DataTemplate>
                            <DataTemplate x:Key="WeatherItem">
                                <TextBlock Text="{Binding DataContext.WeatherText, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0" TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding DataContext.WeatherDetailText, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                            </DataTemplate>
                            <DataTemplate x:Key="DiskItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.DiskText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="DateItem">
                                <TextBlock Text="{Binding DataContext.DateText, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"
                                           Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                            </DataTemplate>
                            <DataTemplate x:Key="CpuItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.CpuText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="GpuItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.GpuText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="MicItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.MicText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="CamItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.CamText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="RamItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.RamText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="NetItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.NetText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.NetTextUp, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center" Margin="3,0,0,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <Polyline Points="{Binding DataContext.NetCurvePoints, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              Stroke="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"
                                              StrokeThickness="1.5" Width="64" Height="14" Stretch="None" Margin="6,0,0,0"
                                              StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="BatteryItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.BatteryText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="VolumeItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.VolumeText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="CapsLockItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.CapsLockText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="ClipboardItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.ClipboardSummary, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="TodoItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.TodoSummary, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="InputMethodItem">
                                <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,10,0"
                                        Click="InputMethodItem_Click"
                                        ToolTip="{Binding DataContext.InputMethodHint, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                        <TextBlock Text="{Binding DataContext.InputMethodText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                            <DataTemplate x:Key="QuickTogglesItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0"
                                            ToolTip="{Binding DataContext.QuickTogglesHint, RelativeSource={RelativeSource AncestorType=Window}}">
                                    <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,4,0" Tag="wifi" Click="QuickToggle_Click">
                                        <TextBlock Text="{Binding DataContext.QuickWifiText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,4,0" Tag="bluetooth" Click="QuickToggle_Click">
                                        <TextBlock Text="{Binding DataContext.QuickBtText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,4,0" Tag="night" Click="QuickToggle_Click">
                                        <TextBlock Text="{Binding DataContext.QuickNightText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,0,0" Tag="mute" Click="QuickToggle_Click">
                                        <TextBlock Text="{Binding DataContext.QuickMuteText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="TimerItem">
                                <Button Style="{StaticResource IconButton}" Width="Auto" Height="Auto" MinWidth="0" Padding="6,3" Margin="0,0,10,0"
                                        Click="TimerItem_Click"
                                        ToolTip="{Binding DataContext.TimerToolTip, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                        <TextBlock Text="{Binding DataContext.TimerText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                        <TextBlock Text="&#xE769;" FontFamily="Segoe MDL2 Assets" FontSize="9" VerticalAlignment="Center" Margin="3,0,0,0"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                                   Visibility="{Binding DataContext.TimerPaused, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                            <DataTemplate x:Key="ScheduleItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.ScheduleSummary, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="HolidayItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.HolidayText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="MeetingItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.MeetingText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="ScreenCapItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.ScreenshotStatusText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="RecordingItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.RecordingText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="VolumeTempItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.VolumeTempText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="UsageItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.UsageMergeText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="FileCopyItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.FileCopyText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="DownloadItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                    <TextBlock Text="{Binding DataContext.DownloadText, RelativeSource={RelativeSource AncestorType=ItemsControl}}" FontSize="11" VerticalAlignment="Center"
                                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate x:Key="SongItem">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="40" Height="40" CornerRadius="12" Margin="0,0,10,0"
                                            Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}">
                                        <Grid>
                                            <Image Source="{Binding DataContext.Artwork, RelativeSource={RelativeSource AncestorType=Window}}" Stretch="UniformToFill"
                                                   Visibility="{Binding DataContext.Artwork, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource NullToVis}}"/>
                                            <TextBlock Text="{Binding Icon}" FontFamily="{Binding Icon, Converter={StaticResource GlyphFont}}" FontSize="18" Foreground="White"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Visibility="{Binding DataContext.Artwork, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource NullToVisInvert}}"/>
                                        </Grid>
                                    </Border>
                                    <!-- 多行模式（默认）：歌名 / 歌手 / 歌词 / 进度条 -->
                                    <StackPanel x:Name="MultiLineInfo" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Grid MaxWidth="220" ClipToBounds="True">
                                            <TextBlock Text="{Binding DataContext.Title, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="13" FontWeight="SemiBold"
                                                       Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                                       karaoke:Marquee.IsEnabled="{Binding MarqueeEnabled, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                            <!-- 播放来源徽标（如 Spotify / 网易云音乐），无来源名时隐藏 -->
                                            <Border CornerRadius="6" Background="#22FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" Padding="4,1" Margin="6,0,0,0" VerticalAlignment="Center"
                                                    Visibility="{Binding DataContext.HasSourceDetail, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="{Binding DataContext.SourceDetail, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="9" MaxWidth="70" TextTrimming="CharacterEllipsis"
                                                           Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding DataContext.Artist, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="11" Margin="0,1,0,0"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <Grid ClipToBounds="True">
                                            <karaoke:KaraokeTextBlock
                                                KaraokeText="{Binding DataContext.CurrentLyricText, RelativeSource={RelativeSource AncestorType=Window}}"
                                                HighlightFraction="{Binding DataContext.CompactHighlightFraction, RelativeSource={RelativeSource AncestorType=Window}}"
                                                IsPlaying="{Binding DataContext.IsPlaying, RelativeSource={RelativeSource AncestorType=Window}}"
                                                HighlightBrush="White" BaseBrush="#80FFFFFF"
                                                karaoke:Marquee.IsEnabled="{Binding MarqueeEnabled, RelativeSource={RelativeSource AncestorType=Window}}"
                                                FontSize="12.5" Margin="0,2,0,0" TextAlignment="Left" FontWeight="SemiBold" Opacity="0.95"/>
                                        </Grid>
                                        <ProgressBar Height="3" Maximum="1"
                                                     Value="{Binding DataContext.Progress, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Margin="0,4,0,0">
                                            <ProgressBar.Template>
                                                <ControlTemplate TargetType="ProgressBar">
                                                    <Grid>
                                                        <Border CornerRadius="2" Background="#33FFFFFF"/>
                                                        <Border x:Name="PART_Indicator" CornerRadius="2" HorizontalAlignment="Left"
                                                                Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </ProgressBar.Template>
                                        </ProgressBar>
                                    </StackPanel>
                                    <!-- 单行模式：封面 + 歌名 - 歌手（一行） -->
                                    <StackPanel x:Name="SingleLineInfo" Orientation="Horizontal" VerticalAlignment="Center" Visibility="Collapsed">
                                        <Grid MaxWidth="140" ClipToBounds="True">
                                        <TextBlock Text="{Binding DataContext.Title, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                                   karaoke:Marquee.IsEnabled="{Binding MarqueeEnabled, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                        <TextBlock Text="{Binding DataContext.Artist, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="11" Margin="6,0,0,0"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                                   TextTrimming="CharacterEllipsis" MaxWidth="100" VerticalAlignment="Center"/>
                                        <TextBlock Text="&#x00B7;" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center"
                                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                                   Visibility="{Binding DataContext.HasLyrics, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"/>
                                        <Grid MaxWidth="300" ClipToBounds="True"
                                        Visibility="{Binding DataContext.HasLyrics, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                        <karaoke:KaraokeTextBlock
                                            KaraokeText="{Binding DataContext.CurrentLyricText, RelativeSource={RelativeSource AncestorType=Window}}"
                                            HighlightFraction="{Binding DataContext.CompactHighlightFraction, RelativeSource={RelativeSource AncestorType=Window}}"
                                            IsPlaying="{Binding DataContext.IsPlaying, RelativeSource={RelativeSource AncestorType=Window}}"
                                            HighlightBrush="White" BaseBrush="#A0FFFFFF" FontSize="12" Margin="8,0,0,0"
                                            TextAlignment="Left" FontWeight="SemiBold"
                                            karaoke:Marquee.IsEnabled="{Binding MarqueeEnabled, RelativeSource={RelativeSource AncestorType=Window}}"
                                            VerticalAlignment="Center"/>
                                    </Grid>
                                    </StackPanel>
                                </StackPanel>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding SingleLineMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter TargetName="MultiLineInfo" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="SingleLineInfo" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.Resources>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                <ContentControl Focusable="False" Content="{Binding}">
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Setter Property="ContentTemplate" Value="{StaticResource SongItem}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Kind}" Value="Time">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource TimeItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Weather">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource WeatherItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Date">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource DateItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Disk">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource DiskItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="InputMethod">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource InputMethodItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="QuickToggles">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource QuickTogglesItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Cpu">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource CpuItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Gpu">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GpuItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Mic">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource MicItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Cam">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource CamItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Ram">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource RamItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Net">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource NetItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Battery">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource BatteryItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Volume">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource VolumeItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="CapsLock">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource CapsLockItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Clipboard">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource ClipboardItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Todo">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource TodoItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Timer">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource TimerItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Schedule">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource ScheduleItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Holiday">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource HolidayItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Meeting">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource MeetingItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="ScreenCap">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource ScreenCapItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Recording">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource RecordingItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="VolumeTemp">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource VolumeTempItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Usage">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource UsageItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="FileCopy">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource FileCopyItem}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Kind}" Value="Download">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource DownloadItem}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </StackPanel>

                    <!-- 按钮靠右 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding HasMedia, Converter={StaticResource BoolToVis}}">
                        <!-- 声音波纹：随音量起伏（紧凑行） -->
                                                <StackPanel x:Name="WaveBoxCompact" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0"
                                    Visibility="{Binding HasWave, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                        <StackPanel x:Name="WaveBarsPanelCompact" Orientation="Horizontal">
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC1" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC2" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC3" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC4" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC5" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC6" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                            <Border Width="2.2" Height="12" CornerRadius="1.1" VerticalAlignment="Center" Margin="0,0,1.8,0"
                                Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                <Border.RenderTransform><ScaleTransform x:Name="WaveBarC7" ScaleY="0.16"/></Border.RenderTransform>
                            </Border>
                        </StackPanel>
                            <Grid x:Name="WaveSpectrumHostCompact" Width="46" Height="13" VerticalAlignment="Center" Visibility="Collapsed"/>
                            <Grid x:Name="WaveRingHostCompact" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" Visibility="Collapsed"/>
                            <Grid x:Name="WaveParticlesHostCompact" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" Visibility="Collapsed"/>
                        </StackPanel>
                        <Button Style="{StaticResource IconButton}" Command="{Binding PlayPauseCommand}">
                            <TextBlock Text="{Binding PlayPauseGlyph}" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                        </Button>
                        <Button Style="{StaticResource IconButton}" Command="{Binding NextCommand}">
                            <TextBlock Text="&#xE893;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- ═══ 展开内容 ═══ -->
                <ScrollViewer x:Name="ExpandedContent" Grid.Row="0" Grid.RowSpan="2" Margin="0,10,0,0"
                              Visibility="Collapsed" Opacity="0" VerticalScrollBarVisibility="Hidden"
                              PanningMode="VerticalOnly">
                    <ScrollViewer.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ExpandedScale" ScaleX="0.985" ScaleY="0.985"/>
                            <TranslateTransform x:Name="ExpandedTranslate" Y="8"/>
                        </TransformGroup>
                    </ScrollViewer.RenderTransform>
                    <StackPanel>
                    <!-- 上岛推送详情（第三方软件推送，展开时优先级最高，置于顶部） -->
                    <Border x:Name="ExpandedPushCard" Visibility="{Binding HasActivePush, Converter={StaticResource BoolToVis}}"
                            CornerRadius="14"
                            Background="{Binding PushCardBackground, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#221B1B26}"
                            BorderBrush="{Binding PushCardBorder, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#59FFFFFF}"
                            BorderThickness="1" Padding="12,10" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ActivePushIcon}" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"
                                           Foreground="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"
                                           Visibility="{Binding ActivePushHasImage, Converter={StaticResource BoolToVisInvert}}"/>
                                <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,10,0" ClipToBounds="True"
                                        Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"
                                        Visibility="{Binding ActivePushHasImage, Converter={StaticResource BoolToVis}}">
                                    <Image Source="{Binding ActivePushImageSource}" Stretch="UniformToFill"/>
                                </Border>
                                <TextBlock Text="{Binding ActivePushTitle}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"
                                           Foreground="{Binding PushCardForeground, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <TextBlock Text="{Binding ActivePushSubtitle}" FontSize="11.5" Margin="0,2,0,0"
                                       Foreground="{Binding PushCardSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                       TextTrimming="CharacterEllipsis"
                                       Visibility="{Binding ActivePushHasSubtitle, Converter={StaticResource BoolToVis}}"/>
                            <TextBlock Text="{Binding ActivePushBody}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"
                                       Foreground="{Binding PushCardSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                       Visibility="{Binding ActivePushHasBody, Converter={StaticResource BoolToVis}}"/>
                            <ProgressBar Height="4" Maximum="1" Value="{Binding ActivePushProgress, Mode=OneWay}" Margin="0,8,0,0"
                                         Visibility="{Binding ActivePushHasProgress, Converter={StaticResource BoolToVis}}">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border CornerRadius="2" Background="#33FFFFFF"/>
                                            <Border x:Name="PART_Indicator" CornerRadius="2" HorizontalAlignment="Left"
                                                    Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                            <ItemsControl ItemsSource="{Binding ActivePushButtons}" Margin="0,8,0,0"
                                          Visibility="{Binding ActivePushHasButtons, Converter={StaticResource BoolToVis}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource IconButton}" Padding="10,5" Margin="0,0,8,0" Click="PushButton_Click">
                                            <TextBlock Text="{Binding Label}" FontSize="12"/>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- 媒体大卡片模板（Hero）：大封面背景 + 渐变遮罩 + 歌名/歌手/专辑叠加 -->
                    <Border x:Name="HeroCard" CornerRadius="18" Height="190" Margin="0,0,0,10" ClipToBounds="True" Cursor="Hand"
                            Visibility="{Binding ExpandedHeroCard, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Artwork}" Stretch="UniformToFill"
                                        AlignmentX="Center" AlignmentY="Center">
                                <ImageBrush.RelativeTransform>
                                    <ScaleTransform ScaleX="1.06" ScaleY="1.06" CenterX="0.5" CenterY="0.5"/>
                                </ImageBrush.RelativeTransform>
                            </ImageBrush>
                        </Border.Background>
                        <Grid>
                            <!-- 无封面时的底色 / 有封面时轻微压暗，保证白色文字可读 -->
                            <Border Background="#B3101018"/>
                            <!-- 顶部轻微压暗 -->
                            <Border Background="#22000000"/>
                            <!-- 底部渐变遮罩 -->
                            <Border VerticalAlignment="Bottom" Height="128">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#00000000" Offset="0"/>
                                        <GradientStop Color="#B3000000" Offset="0.55"/>
                                        <GradientStop Color="#E6000000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <!-- 信息叠加 -->
                            <StackPanel VerticalAlignment="Bottom" Margin="16,0,16,14">
                                <TextBlock Text="{Binding Title}" FontSize="17" FontWeight="SemiBold"
                                           Foreground="White" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Artist}" FontSize="12.5" Margin="0,2,0,0"
                                           Foreground="White" Opacity="0.88" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Album}" FontSize="11" Margin="0,2,0,0"
                                           Foreground="White" Opacity="0.66" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 大封面 + 标题/歌手/专辑 -->
                    <Grid Visibility="{Binding ExpandedShowArtTitle, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="BigArt" Width="72" Height="72" CornerRadius="18" Cursor="Hand"
                                BorderBrush="{Binding AccentBorderBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#336C5CE7}"
                                BorderThickness="1"
                                Background="{Binding AccentBrush, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=#6C5CE7}">
                            <Grid>
                                <Image Source="{Binding Artwork}" Stretch="UniformToFill"
                                       Visibility="{Binding Artwork, Converter={StaticResource NullToVis}}"/>
                                <TextBlock Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="26" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="{Binding Artwork, Converter={StaticResource NullToVisInvert}}"/>
                            </Grid>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"
                                       Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Artist}" FontSize="12.5" Margin="0,2,0,0"
                                       Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Album}" FontSize="11" Margin="0,2,0,0"
                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>

                    <!-- 进度条 -->
                    <Grid Margin="0,10,0,0" Visibility="{Binding ExpandedShowProgress, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="ProgressSlider" Style="{StaticResource ThinSlider}" Minimum="0" Maximum="1"
                                Value="{Binding Progress, Mode=OneWay}" IsEnabled="{Binding CanSeek}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding PositionText}" FontSize="10"
                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                            <TextBlock Text=" / " FontSize="10" Opacity="0.5"
                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                            <TextBlock Text="{Binding DurationText}" FontSize="10"
                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                        </StackPanel>
                    </Grid>

                    <!-- 控制行 -->
                    <Grid Margin="0,6,0,0" Visibility="{Binding ExpandedShowControls, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <!-- 声音波纹：随音量起伏（展开控制行） -->
                                                        <StackPanel x:Name="WaveBox" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0"
                                        Visibility="{Binding HasWave, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                                <StackPanel x:Name="WaveBarsPanelExpanded" Orientation="Horizontal">
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar1" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar2" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar3" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar4" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar5" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar6" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                                <Border Width="2.6" Height="16" CornerRadius="1.3" VerticalAlignment="Center" Margin="0,0,2.2,0"
                                    Background="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}">
                                    <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                                    <Border.RenderTransform><ScaleTransform x:Name="WaveBar7" ScaleY="0.16"/></Border.RenderTransform>
                                </Border>
                            </StackPanel>
                            <Grid x:Name="WaveSpectrumHostExpanded" Width="58" Height="18" VerticalAlignment="Center" Visibility="Collapsed"/>
                            <Grid x:Name="WaveRingHostExpanded" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0" Visibility="Collapsed"/>
                            <Grid x:Name="WaveParticlesHostExpanded" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0" Visibility="Collapsed"/>
                            </StackPanel>
                            <Button Style="{StaticResource IconButton}" Command="{Binding PreviousCommand}" IsEnabled="{Binding CanPrevious}">
                                <TextBlock Text="&#xE892;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                            </Button>
                            <Button Style="{StaticResource IconButton}" Command="{Binding PlayPauseCommand}">
                                <TextBlock Text="{Binding PlayPauseGlyph}" FontFamily="Segoe MDL2 Assets" FontSize="15"/>
                            </Button>
                            <Button Style="{StaticResource IconButton}" Command="{Binding NextCommand}" IsEnabled="{Binding CanNext}">
                                <TextBlock Text="&#xE893;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                            </Button>
                        </StackPanel>
                        <!-- 音量 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                                    Visibility="{Binding HasVolumeControl, Converter={StaticResource BoolToVis}}">
                            <Slider x:Name="VolumeSlider" Style="{StaticResource ThinSlider}" Width="90" Minimum="0" Maximum="1"
                                    Value="{Binding Volume, Mode=TwoWay}"/>
                        </StackPanel>
                    </Grid>

                    <!-- 媒体来源选择（#3 多播放器切换：多个会话并存时显示，点击循环切换） -->
                    <Button Style="{StaticResource IconButton}" HorizontalAlignment="Center" Padding="10,4" Margin="0,8,0,0"
                            Click="MediaSessionCycle_Click"
                            Visibility="{Binding MediaSessionPickerVisible, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets" FontSize="11.5" VerticalAlignment="Center"
                                       Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"/>
                            <TextBlock Text="{Binding SelectedMediaSessionName}" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"
                                       Foreground="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"/>
                            <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="9" Margin="8,0,0,0" VerticalAlignment="Center"
                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"/>
                        </StackPanel>
                    </Button>

                    <!-- 歌词快捷操作：翻译开关 + 复制当前句 + 时间微调(#4) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0"
                                Visibility="{Binding HasLyrics, Converter={StaticResource BoolToVis}}">
                        <Button Style="{StaticResource IconButton}" Padding="9,3" Margin="0,0,8,0" Click="LyricTranslate_Click"
                                ToolTip="{Binding LyricTranslateHint}">
                            <TextBlock Text="{Binding LyricTranslateText}" FontSize="11"/>
                        </Button>
                        <Button Style="{StaticResource IconButton}" Padding="9,3" Click="CopyCurrentLyric_Click"
                                ToolTip="{Binding LyricCopyHint}">
                            <TextBlock Text="{Binding LyricCopyText}" FontSize="11"/>
                        </Button>
                        <Button Style="{StaticResource IconButton}" Padding="8,3" Margin="6,0,2,0" Click="LyricOffsetDown_Click"
                                ToolTip="{Binding LyricOffsetDownHint}">
                            <TextBlock Text="-0.5s" FontSize="11"/>
                        </Button>
                        <Button Style="{StaticResource IconButton}" Padding="8,3" Margin="0,0,2,0" Click="LyricOffsetUp_Click"
                                ToolTip="{Binding LyricOffsetUpHint}">
                            <TextBlock Text="+0.5s" FontSize="11"/>
                        </Button>
                        <TextBlock Text="{Binding LyricOffsetText}" FontSize="10.5" VerticalAlignment="Center" Margin="2,0,0,0"
                                   Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}" Opacity="0.85"/>
                    </StackPanel>

                    <!-- 歌词（无边框，干净排版） -->
                    <ScrollViewer x:Name="LyricsScroll" Height="180" Margin="0,8,0,0"
                                  VerticalScrollBarVisibility="Hidden"
                                  Visibility="{Binding ExpandedShowLyrics, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                        <ItemsControl x:Name="LyricsList" ItemsSource="{Binding LyricLines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource LyricLineBorder}" HorizontalAlignment="Center">
                                        <StackPanel>
                                            <karaoke:KaraokeTextBlock
                                                KaraokeText="{Binding Text}"
                                                HighlightFraction="{Binding HighlightFraction}"
                                                IsPlaying="{Binding DataContext.IsPlaying, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=False}"
                                                HighlightBrush="{Binding TextPrimary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=White}"
                                                BaseBrush="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                                                Style="{StaticResource LyricLineText}"/>
                                            <!-- 翻译行：随「翻译开关」显隐 -->
                                            <TextBlock Text="{Binding Translation}" FontSize="10.5" Margin="0,1,0,0" Opacity="0.72" HorizontalAlignment="Center"
                                                       Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ShowTranslation}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding HasTranslation}" Value="False">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock Text="{Binding LyricsStatus}" FontSize="12" HorizontalAlignment="Center" Margin="0,48,0,0" Opacity="0.65"
                               Foreground="{Binding TextSecondary, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Gray}"
                               Visibility="{Binding HasLyrics, Converter={StaticResource BoolToVisInvert}}"/>

                    <!-- 快捷操作按钮（展开卡片底部一排，可自定义） -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,2"
                                Visibility="{Binding HasQuickActions, Converter={StaticResource BoolToVis}}">
                        <ItemsControl ItemsSource="{Binding QuickActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource IconButton}" Width="34" Height="34" Margin="0,0,6,0"
                                            ToolTip="{Binding ToolTip}" Tag="{Binding Key}" Click="QuickAction_Click">
                                        <TextBlock Text="{Binding Glyph}" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    </StackPanel>
                </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
