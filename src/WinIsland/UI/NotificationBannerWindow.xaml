<Window x:Class="WinIsland.UI.NotificationBannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="340" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False" Focusable="False">
    <Border x:Name="Card" CornerRadius="14" Margin="10" Padding="14,12"
            Background="#F51B1B26" BorderBrush="#5AFFFFFF" BorderThickness="1"
            MouseLeftButtonUp="Card_MouseLeftButtonUp" Cursor="Hand">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="0" Opacity="0.35" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="IconBox" Width="34" Height="34" CornerRadius="10" VerticalAlignment="Top"
                    Background="#6C5CE7">
                <TextBlock x:Name="IconText" Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock x:Name="TitleText" FontSize="12.5" FontWeight="SemiBold" Foreground="White"
                           TextTrimming="CharacterEllipsis" MaxWidth="250"/>
                <TextBlock x:Name="BodyText" FontSize="11" Foreground="#D9FFFFFF" TextWrapping="Wrap"
                           MaxHeight="66" MaxWidth="250" Margin="0,3,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
