## WinIsland 1.0.3（正式版 / Stable）

一款现代化、多功能的 Windows 灵动岛组件。A modern, multi-functional Dynamic Island widget for Windows.

### 更新内容
- 新增组件：日期、CPU 占用、内存占用、网络速度、电量（可在「组件」设置中分别勾选「无歌曲播放时 / 有歌曲播放时」显示）
- 正在播放通知：切换歌曲时右上角弹出「正在播放」横幅
- 低电量提醒：电量低于阈值时右上角提示（阈值 0-50% 可调，每个充电周期提醒一次）
- 全局快捷键：Ctrl+Alt+P 播放/暂停 · Ctrl+Alt+←/→ 上一首/下一首 · Ctrl+Alt+I 显示/隐藏（可在设置中关闭）
- 通知历史：最近 50 条通知记录，设置 → 通知页可查看 / 清空
- 展开卡片内容自定义：封面、进度条、控制按钮、歌词区可分别开关
- 减少动态效果（无障碍 / 省电）：一键关闭弹簧动画
- 组件设置优化：顺序条只显示已勾选的组件、支持拖拽排序、列表与顺序条支持鼠标滚轮和滚动条
- 全部动画改为非线性缓动（iOS 风格弹簧：快启动 + 轻微回弹 + 慢收尾）；展开 / 缩回动画更丝滑流畅
- 修复：组件顺序无法拖动、组件列表 / 顺序条无法滚动、天气组件偶发消失、展开 / 收起时内容短暂空白、CPU 占用显示错乱、暂停后重启程序歌词高亮跳动、通知横幅被裁切等问题

### 资产 Assets
- Windows x64 / arm64 便携版（单文件自包含，免安装直接运行）
- Windows 通用安装包（Inno Setup，同时支持 x64 与 ARM64，自动按架构安装）

> 便携版为独立 exe 文件，不再提供 ZIP 压缩包。
